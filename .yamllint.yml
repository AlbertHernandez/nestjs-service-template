extends: default

ignore: |
  node_modules/
  pnpm-lock.yaml

rules:
  document-start:
    present: false
  line-length:
    ignore: |
      /.github/actions/**/*.yml
      /.github/workflows/*.yml
      /.github/settings.yml
  truthy:
    ignore: |
      /.github/workflows/*.yml
